{
  "name": "stencilquizmaster",
  "private": true,
  "version": "0.0.1",
  "description": "stencilquizmaster",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch"
  },
  "dependencies": {
    "@ionic/core": "4.0.0-beta.16",
    "firebase": "^5.7.0",
    "rxfire": "^3.2.0",
    "rxjs": "^6.3.3"
  },
  "devDependencies": {
    "@stencil/core": "0.15.2"
  }
}
